[project]
name = "urlscan"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "requests",
    "rich",
]

[project.scripts]
urlscan = "urlscan:main"

[dependency-groups]
dev = [
    "pylint",
]

[tool.uv]
package = false
